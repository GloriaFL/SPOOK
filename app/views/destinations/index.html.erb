<h1>Index :D</h1>
<ul>
  <% @destinations.each do |destination| %>
    <li><%= link_to destination.name, destination_path(destination) %></li>
  <% end %>
</ul>

<%= link_to "Create new destination", new_destinations_path %>
